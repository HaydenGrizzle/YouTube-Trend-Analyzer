Built a Python-based tool that uses the YouTube Data API to analyze public video data across targeted search terms, identifying high-performing content and trends within a specific niche.
The project uses SQL (via DuckDB) to store and query data, identidying high-preforming content and uncoverning trends within a given niche.
